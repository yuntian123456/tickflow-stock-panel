# 与上游 eltdx 最新 3.x 同步: 本插件 provider 已迁移至模块化 API
# (client.codes.all_a_shares / all_etfs / all_indices / helpers.xdxr /
#  helpers.factors / corporate.capital_changes)。
# 上游自 2.0 起移除了旧版扁平入口(get_a_share_codes_all / get_factors / get_xdxr
# / get_codes_all 等), 故锁定 >=3.0.4 以保证 API 一致。
eltdx>=3.0.4
